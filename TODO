* Get an actual address from Mt. Gox (FIXME in greenaddress-pos-tool.conf)
* Fix/test fullscreen display on dijon & ketchup
* It checks if transaction is received, but the amount may be wrong / isn't displayed?
  == "The merchant is expected to use their Bitcoin client to see if the correct amount was sent"
  == From the sounds of the README. So where do we look? Should we write better code?
* test the tunnel
* create launch.desktop for run.sh
  + make it track PID of ensure_ and pos, wait for pos and then kill ensure to close the tunnel
  ++ update install.sh
* get treasury into DNS & purpose
* add secret.conf (shared secret), push to bitcoin-pos public repo without it or greenaddress fork
